যন্ত্র শুরু!() {
    ধরি, কথা: লিখন = পড়!(`কিছু লিখঃ `);
    লিখ!( `তুমি লিখেছঃ ` + কথা);

    ধরি, স_ল: লিখন = পড়!(`একটি সংখ্যা লিখ: `);

    লিখ!( `তোমার লিখা সংখ্যার সাথে ১ যোগ করলে হয়: ` + (লস!(স_ল) + 1));
}
